---
"ccslot": minor
---

Launch slots directly: `ccslot <name> [args…]`, `ccslot run <name>`, and `eval "$(ccslot use <name>)"` to switch the current shell. Slot names that shadow commands are now refused.
